<%= form_for(current_user.frelationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :fmicropost_id, @micropost.id %></div>
  <%= f.submit "Favorite", class: "btn btn-primary" %>
<% end %>