<%= form_for(current_user.frelationships.find_by(fmicropost_id: @micropost.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfavorite", class: "btn" %>
<% end %>